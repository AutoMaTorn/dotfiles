#!/bin/sh
# menu: открыть файл dotfiles
#
# Всё, что git видит в ~/dotfiles — то есть без мусора и без .git. Выбранный
# файл открывается в VS Code.

set -eu

REPO=$HOME/dotfiles

file=$(git -C "$REPO" ls-files | sponux --dmenu) || exit 0

exec code "$REPO/$file"
